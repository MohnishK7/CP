 //TCS NQT
   int n;
   cin >> n;
   int arr1[n] = {0};
   for (int i = 0; i < n; i++) {
      cin >> arr1[i];
   }

   int max = arr1[0];
   for (int i = 0; i < n; i++) {
      if (max < arr1[i]) {
         max = arr1[i];
      }
   }

   cout << max << endl;
}
