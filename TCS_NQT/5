//TCS NQT
   int n;
   cin >> n;
   vector<int> a(n);

   for (int i = 0; i < n; i++) {
      cin >> a[i];
   }
   map<int, int> mpp;
   for (int i = 0; i < n; i++) {
      mpp[a[i]]++;
   }
   for (auto x : mpp) {

      cout << x.first << " " << x.second << endl;
   }

   return 0;
}
