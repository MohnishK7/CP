 //TCS NQT
   string s;
   getline(cin, s);
   string n;
   int m = s.length();

   unordered_map<char, int> mpp;
   for (int i = 0; i < m; i++) {
      mpp[s[i]]++;
   }
   for (auto i : mpp) {
      if (i.second == 1) {
         cout << i.first << " ";
      }
   }

   return 0;
}
