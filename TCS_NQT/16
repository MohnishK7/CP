   //TCS NQT
   string s;
   getline(cin, s);
   string n;
   int m = s.length();

   // reverse the stirng using stack
   for (int i = 0; i < m; i++) {
      if (!(s[i] == '(' || s[i] == ')')) {
         n.push_back(s[i]);
      }
   }

   cout << n;

   return 0;
}
